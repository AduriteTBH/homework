self.chunkData = {
  "javascript window.scrollby": "The JavaScript `window.scrollBy` method provides built-in functionality for web applications.\n\n```javascript\n// Example usage of window.scrollBy\nwindow.scrollBy(/* arguments */);\n```\nThis is highly optimized for performance in modern JS engines.",
  "javascript window.getcomputedstyle": "The JavaScript `window.getComputedStyle` method provides built-in functionality for web applications.\n\n```javascript\n// Example usage of window.getComputedStyle\nwindow.getComputedStyle(/* arguments */);\n```\nThis is highly optimized for performance in modern JS engines."
};
